<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Needed by MiroBootReceiver to write secure settings (re-bind a11y service after boot).
         Granted via ADB: pm grant com.miro.a11y android.permission.WRITE_SECURE_SETTINGS -->
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/ic_menu_view"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Miro"
        tools:targetApi="31">

        <!-- Launcher activity: takes app out of "stopped" state so the
             AccessibilityService can bind. Without this, Android marks the
             app as stopped=true/notLaunched=true and refuses to bind any
             service from it, even if enabled in Accessibility settings. -->
        <activity
            android:name=".MiroLauncherActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Boot receiver: forces accessibility service re-bind after reboot.
             On this tablet (OLAX/Allwinner), Android does not re-bind a11y
             services automatically after boot. This receiver toggles the
             secure settings to force re-binding. Requires WRITE_SECURE_SETTINGS. -->
        <receiver
            android:name=".MiroBootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".MiroAccessibilityService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
